namespace Cryptforge.Combat
{
    // Serialized by value in floor assets; append new kinds, never reorder.
    public enum RoomKind
    {
        Combat = 0,
        Forge = 1,
        Elite = 2,
        Boss = 3
    }
}
