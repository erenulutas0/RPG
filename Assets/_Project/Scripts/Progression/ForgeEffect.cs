namespace Cryptforge.Progression
{
    // Serialized by value in forge option assets; append new effects, never reorder.
    public enum ForgeEffect
    {
        Heal = 0,
        BonusUpgrade = 1
    }
}
