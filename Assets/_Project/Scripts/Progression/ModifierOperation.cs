namespace Cryptforge.Progression
{
    // Serialized by value in upgrade assets; append new operations, never reorder.
    public enum ModifierOperation
    {
        Flat = 0,
        Percent = 1
    }
}
